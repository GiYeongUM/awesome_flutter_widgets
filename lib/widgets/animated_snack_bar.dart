import 'package:flutter/material.dart';

class AnimatedSnackBar extends StatelessWidget {
  const AnimatedSnackBar({Key? key, required this.context}) : super(key: key);

  final BuildContext context;

  @override
  Widget build(context) {
    return Container();
  }
}
